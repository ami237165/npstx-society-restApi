// // import { ref } from "firebase/storage";
// import { storage } from "./firebaseConfig";
// export const imagesRef = ref(storage, 'societyImgs');
// // exportconst imagesRef = firebase.storage().ref('images/' + fileName);
